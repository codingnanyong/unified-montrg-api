apiVersion: v1
kind: Service
metadata:
  name: unified-montrg
  namespace: unified-montrg
  labels:
    app.kubernetes.io/name: unified-montrg
    app.kubernetes.io/component: api
spec:
  type: ClusterIP  # Ingress를 통해 접근
  selector:
    app.kubernetes.io/name: unified-montrg
    app.kubernetes.io/component: api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000

